import { render } from '@testing-library/react';
import SignUpMeetup from '../components/SignUpMeetup'

describe('Tests for signing up on meetups', () => {
  test('Renders SignUpMeetup', () => {
    render(<SignUpMeetup />)
  })

})